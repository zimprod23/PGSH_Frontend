# Design Reference Images

Place screenshots from the Claude design preview here.

These images are the visual source of truth for the UI rebuild.
Each file should be named descriptively: `dashboard-home.png`, `profile-page.png`, `stages-list.png`, etc.

Do not commit large files — use `.gitignore` if needed.
